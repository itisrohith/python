def searchRange(nums, target):
    left = 0
    right = len(nums) - 1
    while left <= right:
        mid = (left + right) // 2
        if nums[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    start = left
    left = 0
    right = len(nums) - 1
    while left <= right:
        mid = (left + right) // 2
        if nums[mid] <= target:
            left = mid + 1
        else:
            right = mid - 1
    end = right
    if start <= end:
        return [start, end]
    else:
        return [-1, -1]
nums = [5,7,7,8,8,10]
target = 8
print(searchRange(nums, target))
